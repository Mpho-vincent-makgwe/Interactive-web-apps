const nickname= "Timmy";
const firstname = "Timothy";

console.log(`Good Morning, ${nickname} || ${firstname}!`);/* I used backticks to enclose the string and used ${} on both of the variables to interpolate them.*/
//Removed double quotes

