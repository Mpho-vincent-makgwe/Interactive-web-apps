const nickname= "Timmy";
const firstname = "Timothy";

console.log(`Good Morning, ${nickname} || ${firstname}!`);/**  I used backticks to enclose the string and used ${concatenation} on both of the variables to interpolate them.*/
//Removed Quotation marks
//$ is used to

